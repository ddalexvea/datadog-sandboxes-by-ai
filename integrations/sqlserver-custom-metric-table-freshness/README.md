# sqlserver-custom-metric-table-freshness

**Full sandbox:** https://github.com/ddalexvea/sqlserver-custom-metric-table-freshness

SQL Server custom metric for monitoring table data freshness using `DATEDIFF` and Datadog Agent `custom_queries`. Includes `agent check sqlserver -l trace --table` verification steps.

