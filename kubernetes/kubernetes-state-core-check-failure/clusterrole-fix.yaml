apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: datadog-cluster-agent
  labels:
    app.kubernetes.io/managed-by: datadog-operator
rules:
# ... other rules ...
- apiGroups:
  - apps
  resources:
  - daemonsets
  - deployments
  - replicasets
  - statefulsets
  - controllerrevisions  # ← ADDED: Permission for controllerrevisions
  verbs:
  - get
  - list  # ← ADDED: Need list permission
  - watch # ← ADDED: Need watch permission
